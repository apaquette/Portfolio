<SectionComponent Title="Skills" Id="skills">
    <div class="d-flex flex-wrap justify-content-start" style="margin-left: -0.5rem;">
        @foreach(var kvp in SkillSet){
            <SkillSetComponent SkillSet="@kvp" />
        }
    </div>
</SectionComponent>